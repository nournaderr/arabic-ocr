# Core image processing
opencv-python>=4.8
numpy>=1.24
scipy>=1.11
scikit-image>=0.21

# Feature extraction
scikit-learn>=1.3
mahotas>=1.4          # Zernike moments (optional, falls back to pure-NumPy)

# Deep learning (optional — only needed for CNNClassifier)
torch>=2.0

# Arabic text rendering
arabic-reshaper>=3.0
python-bidi>=0.4.2
Pillow>=10.0

# Utilities
